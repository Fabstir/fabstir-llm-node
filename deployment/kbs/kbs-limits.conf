# /etc/nginx/conf.d/kbs-limits.conf: http{}-context zones for the /v1/kbs/ location
# (design §13). nginx-kbs.conf is a server{} file and cannot hold zones.
limit_req_zone $binary_remote_addr zone=kbs:1m rate=10r/s;
limit_conn_zone $binary_remote_addr zone=kbs_conn:1m;
