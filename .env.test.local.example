# Test Accounts for Base Sepolia Testnet
# Copy this to .env.test.local and fill in your test private keys

# Test User Account (for creating jobs)
TEST_USER_1_ADDRESS=0x... # Your test user address
TEST_USER_1_PRIVATE_KEY=0x... # Your test user private key

# Test Host Accounts (for providing compute)
# These MUST be registered as hosts in the NodeRegistry contract

# Host 1 - For llm-node-prod-1 (port 8080)
TEST_HOST_1_ADDRESS=0x... # Your first test host address
TEST_HOST_1_PRIVATE_KEY=0x... # Your first test host private key

# Host 2 - For llm-node-prod-2 (port 8083)
TEST_HOST_2_ADDRESS=0x... # Your second test host address
TEST_HOST_2_PRIVATE_KEY=0x... # Your second test host private key

# Optional: Override RPC if not using the default
# RPC_URL=https://base-sepolia.g.alchemy.com/v2/YOUR_API_KEY